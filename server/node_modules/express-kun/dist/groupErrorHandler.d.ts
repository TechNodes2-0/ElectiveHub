import { Application, ErrorRequestHandler, Router } from "express";
declare type RouterOrApplication = Application | Router;
export default function groupErrorHandler(routerOrApp: RouterOrApplication, handler: ErrorRequestHandler): (callback: (router: Router) => void) => void;
export {};
