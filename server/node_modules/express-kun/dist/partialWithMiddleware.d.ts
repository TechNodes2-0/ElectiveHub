import { Router } from "express";
import { RequestHandler } from "express-serve-static-core";
declare type SupportedMiddleware = RequestHandler | RequestHandler[];
declare type RotuerOrMiddleware = Router | SupportedMiddleware;
export default function partialWithMiddleware(middlewares: SupportedMiddleware): (routerOrMiddleware: RotuerOrMiddleware) => void;
export {};
