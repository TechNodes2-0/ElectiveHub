import { Router } from "express";
import { RequestHandler } from "express-serve-static-core";
declare type SupportedMiddleware = RequestHandler | RequestHandler[];
export default function groupMiddleware(router: Router, middlewares: SupportedMiddleware): (callback: (router: Router) => void) => void;
export {};
