import { Router } from "express";
import { RequestHandler } from "express-serve-static-core";
declare type SupportedMiddleware = RequestHandler | RequestHandler[];
export default function withMiddleware(router: Router, middlewares: SupportedMiddleware): Router;
export {};
